{
  "name": "replicated",
  "displayName": "Replicated",
  "description": "Lint your kubernetes manifest files using Replicated linting service",
  "version": "0.0.2",
  "publisher": "replicated",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "icon": "img/replicated_logomark.png",
  "galleryBanner": {
    "color": "#C80000",
    "theme": "dark"
  },
  "activationEvents": [
    "onCommand:replicated.lint.enable",
    "onView:replicatedManifests"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "replicated",
          "title": "Replicated",
          "icon": "img/replicated-sidebar.svg"
        }
      ]
    },
    "views": {
      "replicated": [
        {
          "id": "replicatedActions",
          "name": "Actions"
        },
        {
          "id": "replicatedManifests",
          "name": "Manifests"
        },
        {
          "id": "replicatedCLIStatus",
          "name": "CLI Status",
          "when": "!replicated.cliInstalled"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "replicatedActions",
        "contents": "[$(rocket) Test in Environment](command:replicated.testInEnvironment)\nDeploy and test your manifests in a selected environment."
      },
      {
        "view": "replicatedCLIStatus",
        "contents": "The Replicated CLI is not installed.\n[Install CLI](command:replicated.installCLI)\n[Check Status](command:replicated.checkCLI)"
      }
    ],
    "commands": [
      {
        "command": "replicated.lint.enable",
        "title": "Replicated Lint Enable"
      },
      {
        "command": "replicated.lint.disable",
        "title": "Replicated Lint Disable"
      },
      {
        "command": "replicated.lintAll",
        "title": "Lint All Manifests",
        "icon": "$(play)"
      },
      {
        "command": "replicated.lintFile",
        "title": "Lint File",
        "icon": "$(check)"
      },
      {
        "command": "replicated.refreshManifests",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "replicated.openManifest",
        "title": "Open Manifest"
      },
      {
        "command": "replicated.testInEnvironment",
        "title": "Test in Environment",
        "icon": "$(rocket)"
      },
      {
        "command": "replicated.installCLI",
        "title": "Install Replicated CLI",
        "icon": "$(cloud-download)"
      },
      {
        "command": "replicated.checkCLI",
        "title": "Check CLI Status",
        "icon": "$(refresh)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "replicated.testInEnvironment",
          "when": "view == replicatedManifests",
          "group": "navigation@1"
        },
        {
          "command": "replicated.lintAll",
          "when": "view == replicatedManifests",
          "group": "navigation@2",
          "icon": "$(play)"
        },
        {
          "command": "replicated.refreshManifests",
          "when": "view == replicatedManifests",
          "group": "navigation@3",
          "icon": "$(refresh)"
        }
      ],
      "view/item/context": [
        {
          "command": "replicated.lintFile",
          "when": "view == replicatedManifests && viewItem == manifestFile",
          "group": "inline"
        }
      ],
      "view/title": [
        {
          "command": "replicated.installCLI",
          "when": "view == replicatedCLIStatus",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Replicated",
      "properties": {
        "replicated.manifestsFolder": {
          "type": "string",
          "scope": "resource",
          "default": "manifests",
          "description": "%config.manifestsFolder%"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "dependencies": {
    "got": "11.8.3",
    "tar": "^6.1.13",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/glob": "^8.0.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "^16.18.126",
    "@types/tar": "^6.1.3",
    "@types/vscode": "^1.74.0",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "@vscode/test-electron": "^2.2.0",
    "eslint": "^8.28.0",
    "glob": "^8.0.3",
    "mocha": "^10.1.0",
    "typescript": "^4.9.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jdewinne/vscode-replicated"
  }
}
